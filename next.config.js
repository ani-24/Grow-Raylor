/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: ["https://main--quynhvo.netlify.app/"],
  },
};

module.exports = nextConfig;
